# How to do a real reboot
A real reboot can be achieved by right-clicking the restart button in start menu (on phone, by holding the restart button) or by running the `power r` command in the terminal.